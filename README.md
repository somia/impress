Impress is a temporally segmented data accumulator (a bit like rrdtool) with
customizable data model (a bit like Redis).  It's implemented in Python 2,
backed with Amazon DynamoDB, and interfaced with Thrift.
